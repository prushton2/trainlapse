export interface Vehicles {
    data: {
        bearing: number;
        label: string;
        latitude: number;
        longitude: number;
        speed: number;
        headsign: string;
        name: string;
    }[]
}